#include <stdio.h>
int main()
{
    int a, i = 1;
    scanf("%d", &a);
    printf("\n\n");

    if (a > 0) {
        while (i <= a) {
            printf("%d \n", i);
            i++;
        }
    } else {
        printf("0보다 큰 수 입력!");
    }
    return 0;
}
