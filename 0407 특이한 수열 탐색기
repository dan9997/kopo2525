#include <stdio.h>

int main()
{
    double b = 0, c = 0;
    
    int b2 = 0, b3 = 0, b4 = 0;
    
    scanf("%lf%lf", &b, &c);
    
    if((int)b % 1 == 0 && b > 0 && (int)c % 1 == 0 && c > 0){
        for(b; b <= c; b++){
            b2 = (int)b % 10;
            b3 = ((int)b % 100 - b2) / 10; // 십의 자리
            b4 = ((int)b % 1000 - b3 - b2) / 100; //백의자리
    
            if((int)b % 7  == 0 || (int)b2 % 10 == 7 || (int)b3 % 10 == 7 || (int)b4 % 10 == 7){
               continue;
            }
            printf("숫자: %d \n", (int)b);
            
            if((int)b % 5 == 0)
            {
                break;
            }
        }
    } else {
        printf("1 이상의 정수만 입력 가능.");
    }
   
    return 0;
}
