#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{
    double b = 0, c = 0;

    int bol = 1;
    char num[10];
    scanf("%lf%lf", &b, &c);

    for (int i = b; i < c; i++) {
        bol = 1;
        snprintf(num, sizeof(num), "%d", i);
        for (int j = 0; j < sizeof(num); j++) {
            if(num[j] == '7') {
                bol = 0;
            }     
        }
        if (bol == 0 || i % 7 == 0) {
            continue;
        } else if (i % 5 == 0) {
            break;
        } else {
            printf("%d \n", i);
        }
    }
    return 0;
}
