#include <stdio.h>

int main()
{
 int a = 0, b = 0, c = 0, result = 0;
    
    scanf("%d%d%d", &a, &b, &c);
    
    if(c != 0)
    result = (a + c) * c - (a - b) / c ;
    
    printf("%d",result);
    
	
	0----숙제---
	
	
	int a = 10;
	if (a < 10){
	printf("a는 10보다 작다.");}
	
	return 0;
