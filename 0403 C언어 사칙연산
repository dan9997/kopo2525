#include <stdio.h>

int main()
{
 int a, b, c, result;
    
    scanf("%d %d %d", &a, &b, &c);
    
    if(c != 0)
    result = (a + b) * c - (a - b) / c ;
    
    printf("%d", result);

 return 0;
}
